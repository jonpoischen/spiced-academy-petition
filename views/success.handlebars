<div class="header">
    <div class="youtube-responsive-container">
      <iframe width="560" height="315" src="https://www.youtube.com/embed/uwmeH6Rnj2E?rel=0&autoplay=1" frameborder="0" allow="autoplay; encrypted-media" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>
    </div>
    <h1>Thanks for signing the petition!</h1>
  </div>
{{>image}}
<div class="view">
    <p>You signed:</p>
    <img src="{{sig}}" width="500" height="100">
    <br>
    <a href='/signers'><h3>See the other {{num}} people who signed!<h3></a>
    <a href='/editprofile'><button>Edit Your Profile</button></a><br><br>
    <form action="/removesig" method="post">
        <button>Remove Your Signature</button>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </form><br>
    <form action="/logout" method="post">
        <button>Logout</button>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </form><br>
    <form action="/deleteaccount" method="post">
        <button>Delete Account</button>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </form>
</div>
